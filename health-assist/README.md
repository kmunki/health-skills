# Health Assist

This is the main workspace for the Health Assist project.

## Directory Structure

- `workspace/`: Main workspace directory
  - `narratives/`: Narrative text files
  - `structured/`: Structured data exports
  - `conversations/`: Saved conversations